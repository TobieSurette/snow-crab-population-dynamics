
   // Export results:
   REPORT(mu);
   REPORT(sigma);
   REPORT(mu_imm);
   REPORT(mu_mat);
   REPORT(p_skp);
   REPORT(p_mat);
   REPORT(n_imm);
   REPORT(n_skp);
   REPORT(n_mat);
   REPORT(eta_imm);
   REPORT(eta_rec);
   REPORT(eta_res);
   REPORT(eta_mat);
   REPORT(M_imm);
   REPORT(M_mat);
   REPORT(year_effect);

